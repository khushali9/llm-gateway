apiVersion: v1
kind: Service
metadata:
  name: vllm-fast-nodeport
spec:
  type: NodePort
  selector:
    app: vllm-fast
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800     # host reaches the backend at localhost:30800
